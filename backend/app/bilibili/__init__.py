"""Bili live-WS protocol package.

This package owns the low-level frame pack/unpack primitives for the
Bilibili live-room WebSocket protocol. The actual connection, auth,
heartbeat loop, and message dispatch live elsewhere (T11).
"""
